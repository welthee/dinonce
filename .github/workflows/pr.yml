name: pull requests

on:
  pull_request:
    paths:
      - 'api/**'
      - 'cmd/**'
      - 'deployments/**'
      - 'internal/**'
      - 'pkg/**'
      - 'scripts/**'
      - 'go.*'
      - 'Dockerfile'


jobs:
  build:
    runs-on: ubuntu-latest
    steps:

    - uses: actions/checkout@v2

    - name: Build the Docker image
      run: docker build .
